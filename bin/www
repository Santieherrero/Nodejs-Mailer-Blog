#!/usr/bin/env node

var app = require('./../app');
var port = process.env.PORT || 3000;

app.listen(port, function () {
	if(port === 3000) { console.log("Mailer app node: ON\nLisening on port 3000..."); }
});